\documentclass{article}
\usepackage{tikz}
\usepackage{forloop}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment[{[]}]{tikzpicture}


\newcounter{fra}


\begin{document}

\forloop{fra}{1}{\value{fra} < 144}{%
\par\noindent%
\def\time{(\thefra/2)}%
\begin{tikzpicture}
	% a quadratic-based one:
    %\draw [blue,line width=2.5pt,domain={5*\time+50-(42.5/(36*36)*(\time-36)*(\time-36)+7.5)}:{5*\time+50+(42.5/(36*36)*(\time-36)*(\time-36)+7.5)}, line cap=round] plot ({sin(\x)}, {cos(\x)});
    % hyperbolic length
    % 40 is 50-10, tangent to v-shape formed by (0,50) (36,10) and (72,50)
    % 36 is the middle value, 1/2 of \time
    % 2^2 controls closeness to the tangents
    % 5*\time+50 makes it go from 50 degree for center to 400 degree for center within the amount of time -- calculate based on the time domain
    % sorry for all the magic numbers
    \draw [blue,line width=2.5pt,domain={5*\time+50-(10 + sqrt(((40/36)^2)*(\time - 36)^2 + 2^2))}:{5*\time+50+(10 + sqrt(((40/36)^2)*(\time - 36)^2 + 2^2))}, line cap=round] plot ({0.5*sin(\x)}, {0.5*cos(\x)});
    \path (-.55,-.55) -- (-.55,.55) -- (.55,.55) -- (.55, -.55) -- cycle;
\end{tikzpicture}%
}

\end{document}

